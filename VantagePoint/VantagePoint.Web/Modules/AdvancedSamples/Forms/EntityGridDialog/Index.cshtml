@{
    ViewData["Title"] = "Entity Grid Dialog";
}

@section Head {
}

@section ContentHeader {
    <h1>@ViewData["Title"]<small></small></h1>
}

<div class="alert alert-info alert-dismissable">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
    <p><b>EntityGridDialog</b>, as name implies, is a mixture of EntityGrid and EntityDialog.</p>
    <p>This allows you to edit entities directly inside the grid on a sidebar.</p>
    <p>It also supports auto save when switching between records, or when back button is pressed, 
    optionally with a confirmation dialog.</p>
    <p style="text-align: right;"><b>Source Files:</b> 
        @Html.PremiumSourceFile("Index.cshtml"), 
        @Html.PremiumSourceFile("EntityGridDialog.ts"),
        @{await Html.RenderPartialAsync("~/Modules/AdvancedSamples/PremiumSourceWarning.cshtml");}
    </p>
</div>

<div id="GridDiv"></div>

<script type="text/javascript">
    jQuery(function () {
        new VantagePoint.AdvancedSamples.EntityGridDialog($('#GridDiv'));
        Q.initFullHeightGridPage($('#GridDiv'));
    });
</script>